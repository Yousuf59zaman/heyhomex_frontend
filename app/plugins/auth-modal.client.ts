export default defineNuxtPlugin(() => {
  // This plugin ensures the auth modal composable is available globally
  // The composable is auto-imported by Nuxt 3, but this plugin can be used
  // for any additional global setup if needed in the future
})